name: Apply final unified CarMazium integration

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  integrate:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Temporarily paused while exact pre-applied files are reconciled."
